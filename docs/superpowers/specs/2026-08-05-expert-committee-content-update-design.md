# 专家顾问委员会内容更新设计

## 目标

在现有 IvorySQL 专家顾问委员会页面中更新专家信息和头像，按统一规则重排成员，并增加加入委员会的公开联系入口。页面现有卡片、头像、悬停简介和移动端交互保持不变。

## 专家信息

- 将 `technical-expert` 对应成员从“某技术专家”更新为 `NkYoung`。
- 中文 Title：`PostgreSQL ACE · PG 分会西安用户组负责人`。
- 中文简介：`PostgreSQL ACE，PG 分会西安用户组负责人，公众号“PostgreSQL 运维之道”主理人。`
- 英文 Title 和简介采用上述事实的英文翻译。
- NkYoung 不使用真人头像，继续显示现有空头像占位。
- Denis Lussier 保持现有正确拼写，不改为 Dennis Lussier。

## 排序

- 专家按数据中英文姓名字段的完整字符串 A–Z 排列。
- 排序结果直接写入专家数据源，不在页面运行时增加排序逻辑。
- 成员区说明更新为：`专家按英文姓名首字母顺序排列，排名不分先后。将鼠标移至头像或姓名，即可查看专家简介。`
- 英文页面提供含义一致的说明。

## 头像

- 新增桌面文件中的冷菠、施嘉伟、徐小强头像。
- 使用桌面文件替换付超现有头像。
- Denis Lussier 使用其 LinkedIn 公开个人资料页暴露的公开头像。
- 所有新头像沿用现有处理规则：裁切为 640×640 WebP，保持人脸居中并控制文件体积。
- 更新后预计 27 位专家有真人头像，NkYoung 与虞梓轩保留空头像。
- 桌面上的两张页面截图和两张展会照片不作为专家头像使用。

## 加入委员会

- 在成员网格下方新增一块轻量 CTA，视觉上沿用页面的蓝色强调色和圆角卡片语言。
- 中文文案：`欢迎更多数据库专家加入 IvorySQL 专家顾问委员会，与我们共同推动开源数据库生态发展。`
- 英文页面提供含义一致的文案。
- 联系方式为可点击邮件链接：`mailto:ivorysql1213@gmail.com`。
- CTA 不增加表单、申请流程或新的后端逻辑。

## 验证

- 数据校验确认共有 29 位专家，ID 和姓名无重复。
- 校验数据源按英文姓名字段 A–Z 排列。
- 校验 NkYoung 和虞梓轩是仅有的空头像成员。
- 校验新增与替换头像可被构建产物引用。
- 中英文 Docusaurus 构建成功。
- 在官网本地预览中确认排序说明、CTA、邮件链接、头像和悬停简介正常显示。
