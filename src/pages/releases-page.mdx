---
id: IvorySQL-Releases
title: IvorySQL Releases
---

import React, { useState } from 'react';

export const VersionTable = () => {
  const [activeTab, setActiveTab] = useState('currentTab');

  const currentVersions = [
    {
      date: 'Mar 12, 2026',
      version: 'IvorySQL 5.3 STABLE',
      url: 'https://docs.ivorysql.org/en/ivorysql-doc/v5.3/1.html',
      pg: 'PostgreSQL 18',
      highlight: true,
    },
    {
      date: 'Apr 21, 2026',
      version: 'IvorySQL 1.22 STABLE',
      url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.22/v1.22/1.html',
      pg: 'PostgreSQL 14',
      highlight: false,
    },
  ];

  const historicalVersions = [
    { date: 'Dec 18, 2025',  version: 'IvorySQL 5.1 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v5.1/v5.1/1.html' },
    { date: 'Nov 25, 2025',  version: 'IvorySQL 5.0 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v5.0/v5.0/1.html' },
    { date: 'Sept 10, 2025', version: 'IvorySQL 4.6 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v4.6/v4.6/1.html' },
    { date: 'Jun 4, 2025',   version: 'IvorySQL 4.5 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v4.5/v4.5/1.html' },
    { date: 'Mar 26, 2025',  version: 'IvorySQL 1.17 STABLE',url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.17/v1.17/1.html'},
    { date: 'Mar 10, 2025',  version: 'IvorySQL 4.4 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v4.4/v4.4/1.html' },
    { date: 'Jan 13, 2025',  version: 'IvorySQL 4.2 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v4.2/v4.2/1.html' },
    { date: 'Dec 23, 2024',  version: 'IvorySQL 4.0 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v4.0/v4.0/1.html' },
    { date: 'Nov 21, 2024',  version: 'IvorySQL 1.8 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.8/v1.8/1.html' },
    { date: 'Sep 26, 2024',  version: 'IvorySQL 3.4 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v3.4/v3.4/1.html' },
    { date: 'Jul 11, 2024',  version: 'IvorySQL 3.3 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v3.3/v3.3/1.html' },
    { date: 'Apr 11, 2024',  version: 'IvorySQL 3.2 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v3.2/v3.2/1.html' },
    { date: 'Jan 26, 2024',  version: 'IvorySQL 3.1 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v3.1/v3.1/1.html' },
    { date: 'Nov 17, 2023',  version: 'IvorySQL 3.0 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v3.0/v3.0/1.html' },
    { date: 'Jun 28, 2023',  version: 'IvorySQL 2.3 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v2.3/v2.3/1.html' },
    { date: 'Mar 29, 2023',  version: 'IvorySQL 2.2 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v2.2/v2.2/1.html' },
    { date: 'Dec 14, 2022',  version: 'IvorySQL 2.1 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v2.1/v2.1/1.html' },
    { date: 'Sep 09, 2022',  version: 'IvorySQL 1.5 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.5/v1.5/1.html' },
    { date: 'Jun 28, 2022',  version: 'IvorySQL 1.4 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.4/v1.4/1.html' },
    { date: 'May 27, 2022',  version: 'IvorySQL 1.3 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.3/v1.3/1.html' },
    { date: 'Feb 28, 2022',  version: 'IvorySQL 1.2 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.2/v1.2/1.html' },
    { date: 'Jan 25, 2022',  version: 'IvorySQL 1.1 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.1/v1.1/1.html' },
    { date: 'Dec 15, 2021', version: 'IvorySQL 1.0 STABLE', url: 'https://docs.ivorysql.org/en/ivorysql-doc/v1.0/v1.0/1.html' },
  ];

  /* ── shared tokens ── */
  const navy  = '#0d1b3e';
  const blue  = '#2f74ff';
  const blue2 = '#1e50c8';
  const card  = { background: '#fff', borderRadius: '12px', border: '1px solid #e5e9f2', boxShadow: '0 2px 12px rgba(15,35,100,0.07)' };

  /* ── Tab button ── */
  const Tab = ({ id, label }) => {
    const active = activeTab === id;
    return (
      <button
        onClick={() => setActiveTab(id)}
        style={{
          padding: '10px 24px',
          border: 'none',
          borderRadius: '8px 8px 0 0',
          cursor: 'pointer',
          fontWeight: 600,
          fontSize: '0.95rem',
          background: active ? '#fff' : 'transparent',
          color: active ? blue : '#6b7280',
          borderBottom: active ? `2px solid ${blue}` : '2px solid transparent',
          transition: 'color 0.18s, background 0.18s',
          marginRight: '4px',
        }}
      >
        {label}
      </button>
    );
  };

  /* ── Current version card ── */
  const CurrentCard = ({ item }) => (
    <a
      href={item.url}
      style={{ textDecoration: 'none', display: 'block' }}
    >
      <div
        style={{
          ...card,
          padding: '28px 32px',
          display: 'flex',
          alignItems: 'center',
          gap: '24px',
          borderLeft: item.highlight ? `4px solid ${blue}` : '4px solid #e5e9f2',
          transition: 'box-shadow 0.2s, transform 0.2s',
          marginBottom: '16px',
          position: 'relative',
          overflow: 'hidden',
        }}
        onMouseEnter={e => { e.currentTarget.style.boxShadow = '0 6px 24px rgba(47,116,255,0.15)'; e.currentTarget.style.transform = 'translateY(-2px)'; }}
        onMouseLeave={e => { e.currentTarget.style.boxShadow = '0 2px 12px rgba(15,35,100,0.07)'; e.currentTarget.style.transform = 'translateY(0)'; }}
      >
        {/* version badge */}
        <div style={{
          minWidth: '56px', height: '56px',
          borderRadius: '14px',
          background: item.highlight
            ? `linear-gradient(135deg, ${blue} 0%, #60a5fa 100%)`
            : 'linear-gradient(135deg, #475569 0%, #64748b 100%)',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          boxShadow: item.highlight ? '0 4px 14px rgba(47,116,255,0.35)' : 'none',
          flexShrink: 0,
        }}>
          <svg width="26" height="26" fill="none" viewBox="0 0 24 24" stroke="#fff" strokeWidth="1.8">
            <path strokeLinecap="round" strokeLinejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
          </svg>
        </div>

        {/* text */}
        <div style={{ flex: 1 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', flexWrap: 'wrap' }}>
            <span style={{ fontSize: '1.2rem', fontWeight: 700, color: '#1e293b' }}>
              {item.version}
            </span>
            {item.highlight && (
              <span style={{
                background: `linear-gradient(90deg, ${blue} 0%, #60a5fa 100%)`,
                color: '#fff', fontSize: '0.72rem', fontWeight: 700,
                padding: '2px 10px', borderRadius: '20px', letterSpacing: '0.04em',
                textTransform: 'uppercase',
              }}>Latest Stable</span>
            )}
          </div>
          <div style={{ marginTop: '6px', display: 'flex', gap: '20px', flexWrap: 'wrap' }}>
            <span style={{ fontSize: '0.88rem', color: '#64748b' }}>Released: {item.date}</span>
            {item.pg && (
              <span style={{ fontSize: '0.88rem', color: '#64748b' }}>Based on {item.pg}</span>
            )}
          </div>
        </div>

        {/* arrow */}
        <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke={blue} strokeWidth="2" style={{ flexShrink: 0, opacity: 0.7 }}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />
        </svg>
      </div>
    </a>
  );

  /* ── Historical version row ── */
  const HistoryRow = ({ item, idx }) => (
    <a
      href={item.url}
      style={{ textDecoration: 'none', display: 'block' }}
    >
      <div
        style={{
          display: 'flex', alignItems: 'center', gap: '16px',
          padding: '14px 20px',
          borderRadius: '8px',
          background: idx % 2 === 0 ? '#f8faff' : '#fff',
          border: '1px solid transparent',
          transition: 'background 0.18s, border-color 0.18s',
          marginBottom: '4px',
        }}
        onMouseEnter={e => { e.currentTarget.style.background = '#eff4ff'; e.currentTarget.style.borderColor = 'rgba(47,116,255,0.2)'; }}
        onMouseLeave={e => { e.currentTarget.style.background = idx % 2 === 0 ? '#f8faff' : '#fff'; e.currentTarget.style.borderColor = 'transparent'; }}
      >
        {/* dot */}
        <div style={{
          width: '8px', height: '8px', borderRadius: '50%',
          background: blue, opacity: 0.45, flexShrink: 0,
        }} />
        <span style={{ flex: 1, fontSize: '0.95rem', fontWeight: 600, color: '#1e293b' }}>
          {item.version}
        </span>
        <span style={{ fontSize: '0.85rem', color: '#94a3b8', minWidth: '120px', textAlign: 'right' }}>
          {item.date}
        </span>
        <svg width="14" height="14" fill="none" viewBox="0 0 24 24" stroke={blue} strokeWidth="2.2" style={{ opacity: 0.5, flexShrink: 0 }}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />
        </svg>
      </div>
    </a>
  );

  return (
    <div style={{ maxWidth: '820px', margin: '0 auto', paddingBottom: '48px' }}>

      {/* ── Hero banner ── */}
      <div style={{
        background: `linear-gradient(135deg, ${navy} 0%, #102268 60%, #0d3070 100%)`,
        borderRadius: '16px',
        padding: '40px 40px 36px',
        marginBottom: '36px',
        position: 'relative',
        overflow: 'hidden',
      }}>
        {/* decorative glow */}
        <div style={{
          position: 'absolute', top: '-30px', right: '-30px',
          width: '200px', height: '200px', borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(47,116,255,0.25) 0%, transparent 70%)',
          pointerEvents: 'none',
        }} />
        <div style={{ position: 'relative', zIndex: 1 }}>
          <div style={{
            display: 'inline-flex', alignItems: 'center', gap: '8px',
            background: 'rgba(47,116,255,0.18)', border: '1px solid rgba(47,116,255,0.35)',
            borderRadius: '20px', padding: '4px 14px', marginBottom: '16px',
          }}>
            <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#60a5fa' }} />
            <span style={{ fontSize: '0.8rem', color: '#93c5fd', fontWeight: 600, letterSpacing: '0.05em', textTransform: 'uppercase' }}>
              Release History
            </span>
          </div>
          <h1 style={{ color: '#fff', margin: '0 0 10px', fontSize: '2rem', fontWeight: 800, lineHeight: 1.25 }}>
            IvorySQL Releases
          </h1>
          <p style={{ color: 'rgba(180,210,255,0.75)', margin: 0, fontSize: '1rem', lineHeight: 1.6 }}>
            Production-ready open-source releases. Always backed by the latest PostgreSQL.
          </p>
        </div>
      </div>

      {/* ── Tabs ── */}
      <div style={{ borderBottom: '1px solid #e5e9f2', marginBottom: '24px' }}>
        <Tab id="currentTab"    label="Current Versions" />
        <Tab id="historicalTab" label="Historical Versions" />
      </div>

      {/* ── Current ── */}
      {activeTab === 'currentTab' && (
        <div>
          {currentVersions.map((item, i) => <CurrentCard key={i} item={item} />)}
        </div>
      )}

      {/* ── Historical ── */}
      {activeTab === 'historicalTab' && (
        <div style={{ ...card, padding: '8px 12px' }}>
          {/* header row */}
          <div style={{
            display: 'flex', alignItems: 'center', gap: '16px',
            padding: '10px 20px 14px',
            borderBottom: '1px solid #e5e9f2', marginBottom: '4px',
          }}>
            <div style={{ width: '8px', flexShrink: 0 }} />
            <span style={{ flex: 1, fontSize: '0.8rem', fontWeight: 700, color: '#94a3b8', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Version</span>
            <span style={{ fontSize: '0.8rem', fontWeight: 700, color: '#94a3b8', textTransform: 'uppercase', letterSpacing: '0.06em', minWidth: '120px', textAlign: 'right' }}>Released</span>
            <div style={{ width: '14px', flexShrink: 0 }} />
          </div>
          {historicalVersions.map((item, i) => <HistoryRow key={i} item={item} idx={i} />)}
        </div>
      )}

      {/* ── Packages ── */}
      <div style={{ marginTop: '40px' }}>
        <h2 style={{ fontSize: '1.25rem', fontWeight: 700, color: '#1e293b', marginBottom: '16px', display: 'flex', alignItems: 'center', gap: '8px' }}>
          <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke={blue} strokeWidth="2">
            <path strokeLinecap="round" strokeLinejoin="round" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
          </svg>
          Packages
        </h2>
        <a href="https://yum.highgo.com/dists/ivorysql-rpms/" style={{ textDecoration: 'none', display: 'block' }}>
          <div
            style={{
              ...card,
              padding: '20px 24px',
              display: 'flex', alignItems: 'center', gap: '20px',
              borderLeft: `4px solid ${blue}`,
              transition: 'box-shadow 0.2s, transform 0.2s',
            }}
            onMouseEnter={e => { e.currentTarget.style.boxShadow = '0 6px 24px rgba(47,116,255,0.15)'; e.currentTarget.style.transform = 'translateY(-1px)'; }}
            onMouseLeave={e => { e.currentTarget.style.boxShadow = '0 2px 12px rgba(15,35,100,0.07)'; e.currentTarget.style.transform = 'translateY(0)'; }}
          >
            <div style={{
              width: '44px', height: '44px', borderRadius: '10px', flexShrink: 0,
              background: `linear-gradient(135deg, ${blue} 0%, #60a5fa 100%)`,
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              boxShadow: '0 3px 10px rgba(47,116,255,0.3)',
            }}>
              <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="#fff" strokeWidth="1.8">
                <path strokeLinecap="round" strokeLinejoin="round" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4" />
              </svg>
            </div>
            <div style={{ flex: 1 }}>
              <div style={{ fontWeight: 700, fontSize: '1rem', color: '#1e293b' }}>HIGHGO</div>
              <div style={{ fontSize: '0.875rem', color: blue, marginTop: '2px' }}>HIGHGO YUM REPOSITORY →</div>
            </div>
          </div>
        </a>
      </div>

      {/* ── Contact banner ── */}
      <div style={{
        marginTop: '28px',
        background: 'linear-gradient(90deg, rgba(47,116,255,0.08) 0%, rgba(96,165,250,0.06) 100%)',
        border: '1px solid rgba(47,116,255,0.18)',
        borderRadius: '10px',
        padding: '16px 24px',
        display: 'flex', alignItems: 'center', gap: '12px',
        flexWrap: 'wrap',
      }}>
        <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke={blue} strokeWidth="2" style={{ flexShrink: 0 }}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
        </svg>
        <span style={{ fontSize: '0.9rem', color: '#334155' }}>
          Want to provide IvorySQL packages?{' '}
          <a href="mailto:support@ivorysql.org" style={{ color: blue, fontWeight: 600 }}>
            Contact support@ivorysql.org
          </a>
        </span>
      </div>
    </div>
  );
};

<VersionTable />
