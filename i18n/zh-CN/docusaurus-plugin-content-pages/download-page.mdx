---
id: download-page
title: 下载
---

import React from 'react';

export const DownloadPage = () => {
  const navy  = '#0d1b3e';
  const blue  = '#2f74ff';
  const card  = { background: '#fff', borderRadius: '12px', border: '1px solid #e5e9f2', boxShadow: '0 2px 12px rgba(15,35,100,0.07)' };

  const downloads = [
    {
      title: 'IvorySQL 软件包',
      description: '从 GitHub 下载最新的 IvorySQL 服务端安装包、源码压缩包及发布资产。',
      url: 'https://github.com/IvorySQL/IvorySQL/releases',
      icon: (
        <svg width="26" height="26" fill="none" viewBox="0 0 24 24" stroke="#fff" strokeWidth="1.8">
          <path strokeLinecap="round" strokeLinejoin="round" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
        </svg>
      ),
      highlight: true,
    },
    {
      title: 'JDBC 驱动',
      description: '下载 IvorySQL JDBC 驱动，用于 Java 应用程序连接 IvorySQL 数据库。',
      url: '/download/postgresql-42.7.10.jar',
      icon: (
        <svg width="26" height="26" fill="none" viewBox="0 0 24 24" stroke="#fff" strokeWidth="1.8">
          <path strokeLinecap="round" strokeLinejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
        </svg>
      ),
      highlight: false,
    },
  ];

  const DownloadCard = ({ item }) => (
    <a href={item.url} target="_blank" rel="noopener noreferrer" style={{ textDecoration: 'none', display: 'block' }}>
      <div
        style={{
          ...card,
          padding: '28px 32px',
          display: 'flex',
          alignItems: 'center',
          gap: '24px',
          borderLeft: item.highlight ? `4px solid ${blue}` : '4px solid #e5e9f2',
          transition: 'box-shadow 0.2s, transform 0.2s',
          marginBottom: '16px',
        }}
        onMouseEnter={e => { e.currentTarget.style.boxShadow = '0 6px 24px rgba(47,116,255,0.15)'; e.currentTarget.style.transform = 'translateY(-2px)'; }}
        onMouseLeave={e => { e.currentTarget.style.boxShadow = '0 2px 12px rgba(15,35,100,0.07)'; e.currentTarget.style.transform = 'translateY(0)'; }}
      >
        {/* icon badge */}
        <div style={{
          minWidth: '56px', height: '56px',
          borderRadius: '14px',
          background: item.highlight
            ? `linear-gradient(135deg, ${blue} 0%, #60a5fa 100%)`
            : 'linear-gradient(135deg, #475569 0%, #64748b 100%)',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          boxShadow: item.highlight ? '0 4px 14px rgba(47,116,255,0.35)' : 'none',
          flexShrink: 0,
        }}>
          {item.icon}
        </div>

        {/* text */}
        <div style={{ flex: 1 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', flexWrap: 'wrap' }}>
            <span style={{ fontSize: '1.2rem', fontWeight: 700, color: '#1e293b' }}>
              {item.title}
            </span>
          </div>
          <div style={{ marginTop: '6px' }}>
            <span style={{ fontSize: '0.88rem', color: '#64748b' }}>{item.description}</span>
          </div>
        </div>

        {/* arrow */}
        <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke={blue} strokeWidth="2" style={{ flexShrink: 0, opacity: 0.7 }}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />
        </svg>
      </div>
    </a>
  );

  return (
    <div style={{ maxWidth: '820px', margin: '0 auto', paddingBottom: '48px' }}>

      {/* ── Hero banner ── */}
      <div style={{
        background: `linear-gradient(135deg, #7D52F4 0%, #5a35c4 60%, #4a28b0 100%)`,
        borderRadius: '16px',
        padding: '40px 40px 36px',
        marginBottom: '36px',
        position: 'relative',
        overflow: 'hidden',
      }}>
        <div style={{
          position: 'absolute', top: '-30px', right: '-30px',
          width: '200px', height: '200px', borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(125,82,244,0.35) 0%, transparent 70%)',
          pointerEvents: 'none',
        }} />
        <div style={{ position: 'relative', zIndex: 1 }}>
          <div style={{
            display: 'inline-flex', alignItems: 'center', gap: '8px',
            background: 'rgba(125,82,244,0.18)', border: '1px solid rgba(125,82,244,0.35)',
            borderRadius: '20px', padding: '4px 14px', marginBottom: '16px',
          }}>
            <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#c4b5fd' }} />
            <span style={{ fontSize: '0.8rem', color: '#c4b5fd', fontWeight: 600, letterSpacing: '0.05em', textTransform: 'uppercase' }}>
              下载
            </span>
          </div>
          <h1 style={{ color: '#fff', margin: '0 0 10px', fontSize: '2rem', fontWeight: 800, lineHeight: 1.25 }}>
            IvorySQL 下载
          </h1>
          <p style={{ color: 'rgba(180,210,255,0.75)', margin: 0, fontSize: '1rem', lineHeight: 1.6 }}>
            下载 IvorySQL 安装包与驱动程序，快速开启兼容 Oracle 的 PostgreSQL 之旅。
          </p>
        </div>
      </div>

      {/* ── Download cards ── */}
      <div>
        {downloads.map((item, i) => <DownloadCard key={i} item={item} />)}
      </div>

      {/* ── Contact banner ── */}
      <div style={{
        marginTop: '28px',
        background: 'linear-gradient(90deg, rgba(47,116,255,0.08) 0%, rgba(96,165,250,0.06) 100%)',
        border: '1px solid rgba(47,116,255,0.18)',
        borderRadius: '10px',
        padding: '16px 24px',
        display: 'flex', alignItems: 'center', gap: '12px',
        flexWrap: 'wrap',
      }}>
        <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke={blue} strokeWidth="2" style={{ flexShrink: 0 }}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
        </svg>
        <span style={{ fontSize: '0.9rem', color: '#334155' }}>
          需要帮助或有疑问？请联系{' '}
          <a href="mailto:support@ivorysql.org" style={{ color: blue, fontWeight: 600 }}>
            support@ivorysql.org
          </a>
        </span>
      </div>
    </div>
  );
};

<DownloadPage />
